{
  "name": "node-mono",
  "private": true,
  "scripts": {
    "setup": "lerna bootstrap",
    "start": "lerna run start",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
